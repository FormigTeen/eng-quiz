SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_LOGIN=9bef88001@smtp-brevo.com
# Replace with your real SMTP key in your local .env (do not commit it)
SMTP_KEY=
# Optional sender (defaults to SMTP_LOGIN)
SMTP_FROM=

# Optional
# MONGO_URI=mongodb://localhost:27017/db_auth_service
# JWT_SECRET=replace-with-a-secret

# Required for password hashing (pepper/seed)
# PASSWORD_SEED=replace-with-strong-random-string
